{
  "name": "peoplenet",
  "version": "1.0.0",
  "scripts": {
    "deploy:local": "hardhat run scripts/deploy/deploy-all.js --network localhost",
    "deploy:test": "hardhat run scripts/deploy/deploy-all.js --network hardhat",
    "verify": "hardhat run scripts/verify/verify-deployment.js",
    "test": "hardhat test",
    "compile": "hardhat compile",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^4.9.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "peoplenet": "file:"
  }
}
